module records
import Text;

type 
	person = record
		age: integer;
		name: array 34 of char;
		hh: real;
	end;

var s, p: person;

func name(p : person)
	Text.WriteString(s.name);
	Text.WriteLn;
	s.name := "new"
end;

func main()

	s.age := 10;
	Text.WriteInt(s.age);
	Text.WriteLn;
	s.hh := 1.1;
	Text.WriteReal(s.hh);
	Text.WriteLn;
	p.name := "name";
	s.name := "str";
	name(s);
	Text.WriteString(s.name);
	Text.WriteLn

end;

end.
